stages:
  - install-environment
  - build
  - test

setup-my-project:
  stage: install-environment
  script:
    - mkdir -p /go/src/27thisotope.ddns.net/mibzman/CourseCorrect-Student
    - cd /go/src/27thisotope.ddns.net/mibzman/CourseCorrect-Student

build-my-project:
  stage: build
  script:
    - ls
    - cd scheduling
    - ls
    - cd ..
    - go get -d
    - go build

test-my-project:
  stage: test
  script:
    - cd CourseCorrect-Student
    - go test